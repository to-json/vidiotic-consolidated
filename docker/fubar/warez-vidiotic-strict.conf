# The counterfactual: /warez/vidiotic/ with no block of its own.
#
# Mounted over warez-vidiotic.conf by `scripts/stage-fubar.sh --strict`, so the
# path falls through to `location /` and inherits the site-wide policy exactly
# as a warez dropped into `_site/` would today.
#
# This file is empty on purpose. It exists so that the claim "the site-wide CSP
# stops /play dead" is something the rig can demonstrate on demand rather than
# something written in a comment and believed. Run the smoke test both ways; the
# difference is the argument for the block.
